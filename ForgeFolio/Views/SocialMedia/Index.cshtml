@model IEnumerable<ForgeFolio.Core.DTOs.SocialMedia.SocialMediaDto>
@{
    ViewData["Title"] = "Sosyal Medya Yönetimi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["PageIcon"] = "📱";
}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Sosyal Medya Hesapları</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
                    <li class="breadcrumb-item active">Sosyal Medya</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<section class="content">
    <div class="container-fluid">
        @await Html.PartialAsync("_TempDataMessages")
        
        <div class="card">
            <div class="card-header" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white;">
                <h3 class="card-title">Sosyal Medya Listesi</h3>
                <div class="card-tools">
                    <a asp-action="CreateSocialMedia" class="btn btn-light btn-sm">
                        <i class="fas fa-plus"></i> Yeni Ekle
                    </a>
                </div>
            </div>
            <div class="card-body">
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Başlık</th>
                            <th>İkon</th>
                            <th>URL</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.Title</td>
                                <td><i class="@item.Icon" style="font-size: 1.2rem;"></i> @item.Icon</td>
                                <td><a href="@item.Url" target="_blank">Link</a></td>
                                <td>
                                    <a asp-action="UpdateSocialMedia" asp-route-id="@item.Id" class="btn btn-info btn-sm">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a asp-action="DeleteSocialMedia" asp-route-id="@item.Id" class="btn btn-danger btn-sm"
                                       onclick="return confirm('Bu kaydı silmek istediğinize emin misiniz?')">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>
