@model ForgeFolio.Core.DTOs.About.AboutDto
@{
    ViewData["Title"] = "Hakkımda";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["BodyClass"] = "page-about";
    ViewData["PageIcon"] = "👤";
}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Hakkımda Bilgileri</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
                    <li class="breadcrumb-item active">About</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<section class="content">
    <div class="container-fluid">
        @await Html.PartialAsync("_TempDataMessages")
        
        <div class="card card-primary">
            <div class="card-header">
                <h3 class="card-title">Hakkımda Düzenle</h3>
            </div>
            <form asp-action="UpdateAbout" method="post">
                <input type="hidden" name="Id" value="@(Model?.Id ?? 0)" />
                <div class="card-body">
                    <div class="form-group">
                        <label>Başlık</label>
                        <input type="text" class="form-control" name="Title" value="@Model?.Title" placeholder="Örn: Full Stack Developer">
                    </div>
                    <div class="form-group">
                        <label>Alt Açıklama</label>
                        <input type="text" class="form-control" name="SubDescription" value="@Model?.SubDescription" placeholder="Kısa açıklama">
                    </div>
                    <div class="form-group">
                        <label>Detaylar</label>
                        <textarea class="form-control" name="Details" rows="5">@Model?.Details</textarea>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-primary">Kaydet</button>
                </div>
            </form>
        </div>
    </div>
</section>
